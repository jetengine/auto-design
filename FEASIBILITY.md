# 方案可行性分析：AI Agent 通过 MCP 驱动 CATIA 三维建模

> 配套文档：[README.md](README.md)
> 分析日期：2026-08-20

---

## 一、总体结论

**技术上可行，但工程落地难度中高，且存在几个"看似合理、实际会踢到铁板"的关键点**。适合作为**内部/半自动辅助建模工具**推进；若目标是**全自动、无人值守、任意复杂零件**，需要显著缩小范围。

综合评分（10 分制）：

| 维度       | 评分   | 说明                                     |
| ---------- | ------ | ---------------------------------------- |
| 技术可行性 | 8 / 10 | CATIA COM + MCP + LLM 各环节都有成熟先例 |
| 工程稳定性 | 6 / 10 | CATIA COM 本身的脆弱性是最大风险         |
| 商业/合规  | 5 / 10 | 授权、脚本安全、企业 PLM 集成会拖慢落地  |
| 建模完备性 | 5 / 10 | 53 个工具覆盖不了 CATIA 的长尾 API       |
| ROI        | 7 / 10 | 在标准件、参数族、变体设计场景下收益明显 |

---

## 二、每一层的可行性拆解

### 2.1 AI Agent 层 —— ✅ 可行，但需要专门的"CAD 规划"能力

**可行的部分**

- LLM 拆解"Sketch → Pad → Fillet"这类线性步骤，目前 GPT-4 级模型已可靠。
- 结构化工具调用（tool calling / JSON schema）是主流能力，无需重造。

**风险点**

- **空间推理弱**：LLM 对三维拓扑、干涉、连续性判断很不稳定，纯语言层几乎无法预测"这个 Loft 会不会自交"。
- **参数化设计意图缺失**：用户说"薄壁件、加强筋沿主受力方向"，LLM 很难把"主受力方向"翻译成正确的草图平面。
- **长链错误累积**：10 步之后 Agent 已经不知道特征树现状，必须每步都做"读取当前状态"，否则会漂移。

**缓解**

- 引入 **世界模型（feature tree 缓存 + 几何摘要）** 每步同步给 Agent。
- 关键决策点做 **human-in-the-loop 复核**（草图、基准、公差）。

### 2.2 MCP 协议层 —— ✅ 完全可行

- STDIO / JSON-RPC 是 MCP 标准形态，成熟。
- 请求 ID 幂等在协议层容易做，但**语义幂等**（重复调用不产生重复特征）要在 CATIA 侧维护"特征名 → ID"的映射表，这是要写的工程量。

### 2.3 CATIA Agent MCP（工具层）—— ⚠️ 最大工程量所在

**"53 个工具"够不够？**

CATIA V5 的 Automation API 有 **数千个类和方法**。53 个工具意味着做了大量抽象合并，能覆盖：

- 覆盖得住：Sketch 基本图元、Pad/Pocket/Shaft、常见 Fillet/Draft、简单 Sweep/Loft、Join、装配约束、测量、导出。
- **覆盖不住**：复杂草图约束求解、Power Copy / UDF、Knowledgeware（关系式、规则、检查）、Generative Shape Design 里的高阶曲面（Bump、Wrap、Styling Sweep）、Sheet Metal、Composites、DMU 高级仿真。

**结论**：53 个工具适合**中等复杂度机械零件 + 少量曲面**；再往上要么扩到 150+ 工具，要么放弃全自动。

**参数校验的现实**

- 路径 / 单位 / 命名可以在 MCP 侧校验。
- 但**几何合法性**（例如"这条草图能否闭合"、"这个引用是否还存活"）只能扔给 CATIA 才知道结果 —— 这就是为什么"证据驱动 + 迭代修复"是必须的。

### 2.4 Windows COM / STA 单线程 —— ⚠️ 现实中最痛的地方

**这是整个方案里最容易翻车的层**。

- **CATIA COM 是有名的脆弱**：会话丢失、ROT 找不到、许可掉线、后台报错弹窗阻塞 UI 线程 —— 这些在真实工厂环境每天都发生。
- **单 STA 串行是对的**，但代价是**吞吐上限极低**（每个零件级建模都在几十秒到几分钟量级），无法用并发把响应做快。
- **无头运行受限**：CATIA V5 不支持真正意义的服务化 / 无 GUI 运行，必须有交互式桌面会话。这意味着：
  - 部署得跑在 Windows Server + RDP 或专用工作站上。
  - 一个物理机通常只跑一个 CATIA 会话。
  - 崩溃恢复要靠"杀进程 + 重启 + 重连 ROT"的看门狗。

**缓解**

- 必备：**看门狗进程 + 超时熔断 + 弹窗自动关闭（EnableCommandGeneration/DisplayFileAlerts=False）**。
- 必备：**每 N 次调用做一次会话健康检查**（拿 `Application.CATIA` 属性做心跳）。
- 生产上建议：**一台建模节点 + 消息队列 + 前置排队**，把并发让位给可靠性。

### 2.5 CATIA V5 原生内核 —— ✅ 价值主张成立

- "保住可编辑特征树"这个价值主张是**正确且稀缺**的 —— 目前大多数"AI + CAD"方案要么直接吐 STEP（死几何），要么用开源 CAD 内核（无法进 CATIA 工程流程）。
- 但要注意：**并非所有原生特征都稳定**。GSD 的 Fill、Blend、Close Surface 在参数变化下经常"能建但更新会失败"，Agent 必须能识别"绿了但下一次 Update 会红"的隐性坑。

### 2.6 安全边界 —— ✅ 思路正确

- 白名单工具、禁止任意 CATScript 注入是对的。
- 补充建议：
  - **文件系统沙箱**：Agent 只能写入指定输出目录。
  - **审计日志**：每次工具调用记录参数、返回、耗时、CATIA 会话快照 —— 出问题能回溯。
  - **模型输出脱敏**：涉密零件的名称/尺寸不要回传给云端 LLM，用本地/私有部署模型或做参数遮罩。

---

## 三、"工程验证 5 项原则"的可行性

| 原则      | 可行性 | 备注                                                                     |
| --------- | ------ | ------------------------------------------------------------------------ |
| 更新检查  | ✅     | `Part.Update` + 捕获异常即可                                             |
| 干涉/间隙 | ✅     | Assembly 下 Clash 分析 API 稳定                                          |
| 连续性    | ⚠️     | GSD 连续性分析 API 有，但结果解析口径不统一，需要自建标准                |
| 单一实体  | ✅     | 拓扑遍历可做                                                             |
| STEP 回读 | ⚠️     | 体积比对易做；**特征丢失**（如小圆角在 STEP 里被合并）是常态，需容差策略 |

**5 项都能做，但"连续性"和"STEP 回读"需要工程侧定义"合格阈值"**，否则要么误报、要么漏报。

---

## 四、关键假设的压力测试

| 方案中的假设                   | 现实检验                                                                       |
| ------------------------------ | ------------------------------------------------------------------------------ |
| "LLM 能把自然语言拆成正确步骤" | 简单件可以；复杂件必须有模板/参数族兜底                                        |
| "53 个工具够用"                | 机加件 OK；钣金、曲面、复合材料不够                                            |
| "单 STA 串行能稳定跑"          | 需要看门狗才能达到"稳定"                                                       |
| "每步都能拿到结构化失败原因"   | CATIA API 的错误信息**质量参差**，很多是 `E_FAIL` 无细节，Agent 修复能力会打折 |
| "STEP 回读可以验证正确性"      | 只能验证宏观几何，验证不了参数化意图                                           |
| "请求 ID 幂等 = 安全重试"      | 需要 CATIA 侧维护特征映射，否则重试会造成脏特征                                |

---

## 五、建议的落地策略

**分阶段收窄目标，比一次做全自动更容易成功：**

1. **阶段 1 · 参数化变体生成**（推荐起点）
   - 已有 CATPart 模板 → Agent 只改参数、少量特征增删。
   - 风险最低、ROI 最高，适合标准件族、系列化产品。

2. **阶段 2 · 从零建标准零件**
   - 限定在 Part Design + 简单 GSD。
   - 引入模板库和"CAD 常识规则"（草图必须全约束、Pad 前必须有平面等）。

3. **阶段 3 · 复杂曲面 / 装配 / 工程图**
   - 扩到 150+ 工具，引入专用曲面 Agent。
   - 需要真正的空间推理辅助（可加几何求解器或规则引擎）。

4. **持续**：审计、看门狗、回归测试库（一组黄金零件，每次工具变更全部重跑）。

---

## 六、风险登记表（Top 10）

| #   | 风险                       | 严重度 | 概率 | 缓解措施                       |
| --- | -------------------------- | ------ | ---- | ------------------------------ |
| 1   | CATIA 会话崩溃 / ROT 丢失  | 高     | 高   | 看门狗 + 自动重启 + 会话心跳   |
| 2   | LLM 对复杂几何拆解错误     | 高     | 高   | 模板兜底 + 人在环 + 世界模型   |
| 3   | 弹窗阻塞 UI 线程导致死锁   | 高     | 中   | 关闭所有交互提示 + 超时熔断    |
| 4   | 工具集覆盖不足需要临时扩容 | 中     | 高   | 预留扩展机制，回归测试保护存量 |
| 5   | STEP 回读误报/漏报         | 中     | 中   | 定义分级容差，只报宏观差异     |
| 6   | 许可 / 席位不足            | 中     | 中   | 建模节点独占许可，排队串行     |
| 7   | 涉密数据出网               | 高     | 中   | 私有部署模型 + 参数遮罩 + 审计 |
| 8   | 特征树重试造成脏特征       | 中     | 中   | 语义幂等映射表 + 事务式回滚    |
| 9   | GSD 曲面"能建不能更新"     | 中     | 高   | 每步显式 `Update`，失败即回退  |
| 10  | PLM/ENOVIA 集成阻塞        | 高     | 中   | 先做离线模式，后接入 PLM       |

---

## 七、一句话总结

> **架构方向是对的**，尤其是"原生特征 + 证据驱动 + 强类型工具"这三条原则；
> **翻车风险主要不在 AI，而在 CATIA COM 的运行时稳定性**；
> **成功的关键**是"缩小自动化范围 + 建看门狗 + 建回归零件库"，而不是继续堆工具数量。
